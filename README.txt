1) Copy .icatrc to your $HOME and adjust the path to the ICAT source code.

2) Update the path to amplxe-vars.sh (/opt/intel/vtune_amplifier_xe_2017.4.0.518798/amplxe-vars.sh) in two files: memory_mode_other.sh and memory_mode_mpi.sh

3) Update the path to the "clusterAdvisor" executable in cluster_mode.sh

